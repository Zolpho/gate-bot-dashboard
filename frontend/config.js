// GitHub Pages frontend configuration.
// Docker rewrites this file at container startup and defaults to same-origin API access.
window.GATE_DASHBOARD_CONFIG = Object.freeze({
  apiBaseUrl: "https://gatebots-api.eqty.pro",
});
